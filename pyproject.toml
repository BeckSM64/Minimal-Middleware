[build-system]
requires = [
    "scikit-build-core",
    "pybind11"
]
build-backend = "scikit_build_core.build"

[project]
name = "mmw"
version = "2.2.0"

[tool.scikit-build]
wheel.packages = ["python/mmw"]

[tool.scikit-build.cmake.define]
BUILD_PYTHON_MODULE = "ON"
